/// @description Insert description here
// You can write your code in this editor


/*if p_deb != undefined {
	my_x = path_get_point_x(p_deb, 0)
	my_y = path_get_point_y(p_deb, 0)
	show_debug_message(my_x)
	show_debug_message(my_y)
	draw_path(p_deb, o_hole.x,o_hole.y, true)
	for (var i = 0; i <= path_get_length(p_deb); ++i) {
	    my_x = path_get_point_x(p_deb, i)
		my_y = path_get_point_y(p_deb, i)
		draw_sprite(s_test_6x6, 0, my_x*6, my_y*6)
	}
	
}*/


	